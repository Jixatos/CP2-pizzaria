spring:
  datasource:
    url: jdbc:oracle:thin:@oracle.fiap.com.br:1521:ORCL
    username: ${FIAP_USERNAME}
    password: ${FIAP_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false
    show-sql: true
